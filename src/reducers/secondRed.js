const findname=''

const secondRed=(state=findname, action)=>{
    switch (action.type) {
        case 'IMPUT':
            
            return state= action.inp

        default: 
            return state
        
    }
}
export default secondRed ;
